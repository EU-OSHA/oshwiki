display:
  default:
    display_options:
      exposed_form:
        options:
          exposed_sorts_label: 'Сортирај по'
      fields:
        state:
          label: State
        label:
          label: Label
        translator:
          label: Provider
        changed:
          label: Changed
        operations:
          label: Operations
      filters:
        state:
          expose:
            label: State
        translator:
          expose:
            label: Provider
